apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pg-data
  name: pg-data
  namespace: odl-ckan-sandbox-ns
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

